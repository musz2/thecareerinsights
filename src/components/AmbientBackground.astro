---
/* Premium ambient background: a slow "talent network" constellation —
   nodes (people/skills) connecting into a living network, in brand colors.
   Lightweight 2D canvas (no WebGL, no Three.js), screen-blended so it
   glows on dark chapters and disappears on light editorial sections.
   Removed on mobile / coarse pointers / reduced motion. */
---

<div class="ambient-stage" data-ambient-stage aria-hidden="true">
  <canvas data-ambient-canvas></canvas>
</div>
